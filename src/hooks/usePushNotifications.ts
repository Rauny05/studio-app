"use client";
// Push notifications are delivered via ntfy.sh to the user's phone.
// This hook is a no-op — no FCM registration needed.
// Notifications arrive via the ntfy Android app subscribed to the private topic.
export function usePushNotifications() {}
